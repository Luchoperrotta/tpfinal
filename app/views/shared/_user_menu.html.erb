  
<div class="collapse navbar-collapse show" id="navbarWithDropdown">
  <ul class="navbar-nav">
    <% if user_signed_in? %>
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle btn btn-secondary " href="#" id="navbarDropdownMenuLink" role="button" data-bs-toggle="dropdown" aria-expanded="false">
          <span style="color:white"><strong><%= current_user.last_name %>, </strong><%= current_user.first_name %></span>
        </a>
        <ul class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
          <li><%= link_to "Editar Perfil", edit_user_registration_path, class: "btn btn text-white btn-success" %></li>
          <hr>
          <li><%= link_to "Cerrar Sesión", destroy_user_session_path, class: "btn btn text-white btn-success",  method: :delete  %></li>
        </ul>
      </li>
    <% else %>
      <%= link_to "Iniciar Sesión", new_user_session_url, class: 'btn text-white btn-success' %>
    <% end %>
  </ul>
</div>
