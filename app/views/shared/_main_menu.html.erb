<nav class="navbar navbar-expand-xl navbar-light text-white bg-primary">
  <div class="container-fluid">
    <a class="navbar-brand"  href="<%= root_url %>">MarketUap</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarWithDropdown" aria-controls="navbarWithDropdown" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse show " id="navbarWithDropdown">
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="products" if can? :manage >Productos</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="profiles" if can? :manage >Mi Perfil</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="#">Carrito</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="profits" if can? :manage >Beneficios</a>
        </li>
        <!-- Dropdown -->
    <li class="nav-item dropdown">
      <a class="nav-link dropdown-toggle" href=" <%= categories_url %>" id="navbarDropdownMenuLink" role="button" data-bs-toggle="dropdown" aria-expanded="false">Categorías</a>
      <ul class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
        <li><%= link_to "Editar Categorias", categories_url, class: 'dropdown-item' if can? :index, Category %></li>
        <% Category.all.each do |cat| %>
          <li><%= link_to cat.name, store_products_url(category: cat.id), class: 'dropdown-item' %></li>
        <% end %>
      </ul>
    </li>
    <li>
        <li>
      <%= link_to "Administrators", administrators_url, class: 'nav-link' if can? :index, :administrator %>
    </li>



        <%= form_tag(products_path, method: :get) do %>
  	<%= text_field_tag(:search, params[:search]) %>
  	<%= submit_tag("Buscar") %>
	<% end %>

      </ul>
      
    </div>
    
  </div>
  <%= render 'shared/user_menu' %>
</nav>